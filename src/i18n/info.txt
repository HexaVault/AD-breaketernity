https://www.loc.gov/standards/iso639-2/php/code_list.php

Use ISO 639-1 where possible, ISO 639-2 otherwise, else use whatever is reasonable and NOT already on that list

$?aX (where ? is a number, starts at 1 goes up for each occurance) is placeholder and is replaced

$, without ?aX after it, is used to split strings into multiple without using tons of string unnecessarily